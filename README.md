# WebP to PNG Converter

## Cài đặt

```bash
pip install -r requirements.txt
Cách sử dụng
Tạo thư mục input_images cùng cấp với file main.py.

Đặt các file ảnh .webp vào thư mục input_images.

Chạy script:
python main.py
Lấy kết quả ảnh .png tại thư mục output_images.
